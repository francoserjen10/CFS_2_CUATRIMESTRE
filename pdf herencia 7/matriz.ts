class Matriz {
    //atributos

    constructor() {
        // llenar la matriz con random o fija
    }

    getValor(x: number, y: number): number {
        return // this.patente;
    }
}

let mat = new Matriz();
let valor = mat.getValor(2, 4);
console.log(valor);